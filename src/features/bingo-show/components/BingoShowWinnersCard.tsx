/**
 * BingoShowWinnersCard.tsx — DESCONTINUADO. O card fixo/persistente foi rejeitado pelo
 * usuário ("essa cagada, refaca") e substituído por `BingoShowWinnerPopup.tsx`, que abre
 * como popup ao receber um winner novo (evento SSE) e fecha sozinho pouco depois, sem
 * ocupar espaço fixo no layout do sorteio. Arquivo mantido vazio (sem export ativo, sem
 * uso em `components/index.ts` nem em `BingoShowDrawScreen.tsx`) só porque a exclusão do
 * arquivo em si não foi autorizada nesta sessão — ver `BingoShowWinnerPopup.tsx` para a
 * versão atual.
 */
export {};
